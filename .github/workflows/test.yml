name: Test formula

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  install-and-run:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - name: Register checkout as tap
        run: brew tap unitedideas/tap "file://$GITHUB_WORKSPACE"
      - name: Install formula
        run: brew install unitedideas/tap/actablesite-check
      - name: Run formula test
        run: brew test actablesite-check
      - name: Check a live public robots file
        run: actablesite-check actablesite.com --json | node -e 'let s=""; process.stdin.on("data", d => s += d); process.stdin.on("end", () => { const r = JSON.parse(s); if (r.crawlers.length !== 8) process.exit(1); })'
